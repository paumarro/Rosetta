# Authentication service URL
VITE_AUTH_URL=http://localhost/auth

# Backend-editor API URL for Vite proxy (optional)
# Default: http://localhost:3001 (works for both load testing and docker-compose dev)
# Override only if backend-editor is running on a different port
# VITE_EDITOR_API_URL=http://localhost:3001

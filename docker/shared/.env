# Rosetta Platform - Environment Variables Template
# =================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to git - it contains secrets!
# The .env file is loaded automatically by Docker Compose.

# ─────────────────────────────────────────────────────────────────────────────
# MICROSOFT ENTRA ID (Azure AD) - OAuth/OIDC Configuration
# ─────────────────────────────────────────────────────────────────────────────
# Get these from Azure Portal → App Registrations → Your App

# Tenant ID: Found in Azure AD Overview or App Registration
# Format: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
TENANT_ID=1f6e9eec-eeb0-492e-9326-ea74eac39abc

# Client ID: Your application's client ID (also called Application ID)
# Format: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
CLIENT_ID=55689c46-8192-4982-9c8d-468d2f686322

# Client Secret: Create under "Certificates & secrets" in Azure
# IMPORTANT: This is shown only once when created - save it!
CLIENT_SECRET=_ko8Q~s6Drc6LGWDTv1Lt6bK-SEP7u33Ww5bFadY

# ─────────────────────────────────────────────────────────────────────────────
# DATABASE CREDENTIALS
# ─────────────────────────────────────────────────────────────────────────────
# PostgreSQL (used by BE for users, learning paths)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=rosetta

# MongoDB (used by be-editor for diagrams)
# Default uses no authentication - for production, add auth!
MONGODB_URI=mongodb://mongodb:27017/rosetta-editor

# ─────────────────────────────────────────────────────────────────────────────
# SERVICE URLS (used for internal communication)
# ─────────────────────────────────────────────────────────────────────────────
# These are Docker internal DNS names - no need to change for standard setup
AUTH_SERVICE_URL=http://auth-service:3002
BE_API_URL=http://backend:8080

# ─────────────────────────────────────────────────────────────────────────────
# DOMAIN CONFIGURATION
# ─────────────────────────────────────────────────────────────────────────────
# Domain for cookies and redirects
# Development: localhost
# Production: your-domain.com
ROSETTA_DOMAIN=localhost

# Full OAuth redirect URI (must match Azure AD config exactly!)
# Development: http://localhost/auth/callback
# Production: https://your-domain.com/auth/callback
OIDC_REDIRECT_URI=http://localhost/auth/callback

# ─────────────────────────────────────────────────────────────────────────────
# OPTIONAL: Development/Debug Settings
# ─────────────────────────────────────────────────────────────────────────────
# Set to "true" to enable development mode (relaxed security, verbose logs)
DEVELOPMENT=true

# Go gin framework mode: debug, release, test
GIN_MODE=debug

# Node environment
NODE_ENV=development

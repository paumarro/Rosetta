# Rosetta Platform - Main Docker Compose
# All services with nginx reverse proxy
#
# ROUTES (via nginx on port 80):
#   /api/*     → backend
#   /editor/*  → be-editor
#   /studio/*  → fe-editor
#   /*         → frontend

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.docker.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
 
      - be-editor-1
      - be-editor-2
      - be-editor-3
      - backend-1
      - backend-2
      - backend-3
      - auth-service
    networks:
      - rosetta
    restart: unless-stopped

  frontend:
    build:
      context: ..
      dockerfile: apps/frontend/Dockerfile
      args:
        VITE_BE_API_URL: http://localhost
        VITE_DEV_EDITOR_FE_URL: /studio/
        VITE_AUTH_URL: http://localhost
    expose:
      - "80"
    networks:
      - rosetta

  fe-editor:
    build:
      context: ..
      dockerfile: apps/frontend-editor/Dockerfile
    expose:
      - "80"
    networks:
      - rosetta

  # Backend Load Balancing - 3 replicas
  backend-1:
    build: ../services/backend
    container_name: backend-1
    expose:
      - "8080"
    environment:
      - RUNNING_IN_DOCKER=true
      - DB_HOST=postgres
      - CLIENT_ID=${CLIENT_ID}
      - TENANT_ID=${TENANT_ID}
      - PG_DB_URL=${PG_DB_URL}
      - EDITOR_BASE_URL=${EDITOR_BASE_URL:-http://nginx/editor}
      - ROSETTA_DOMAIN=${ROSETTA_DOMAIN}
      - ROSETTA_FE=${ROSETTA_FE}
      - COMMUNITY_GROUP_MAPPINGS=${COMMUNITY_GROUP_MAPPINGS:-}
      - GRAPH_SYNC_INTERVAL_HOURS=${GRAPH_SYNC_INTERVAL_HOURS}
      - ADMIN_EMAILS=${ADMIN_EMAILS:-}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - rosetta

  backend-2:
    build: ../services/backend
    container_name: backend-2
    expose:
      - "8080"
    environment:
      - RUNNING_IN_DOCKER=true
      - DB_HOST=postgres
      - CLIENT_ID=${CLIENT_ID}
      - TENANT_ID=${TENANT_ID}
      - PG_DB_URL=${PG_DB_URL}
      - EDITOR_BASE_URL=${EDITOR_BASE_URL:-http://nginx/editor}
      - ROSETTA_DOMAIN=${ROSETTA_DOMAIN}
      - ROSETTA_FE=${ROSETTA_FE}
      - COMMUNITY_GROUP_MAPPINGS=${COMMUNITY_GROUP_MAPPINGS:-}
      - GRAPH_SYNC_INTERVAL_HOURS=${GRAPH_SYNC_INTERVAL_HOURS}
      - ADMIN_EMAILS=${ADMIN_EMAILS:-}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - rosetta

  backend-3:
    build: ../services/backend
    container_name: backend-3
    expose:
      - "8080"
    environment:
      - RUNNING_IN_DOCKER=true
      - DB_HOST=postgres
      - CLIENT_ID=${CLIENT_ID}
      - TENANT_ID=${TENANT_ID}
      - PG_DB_URL=${PG_DB_URL}
      - EDITOR_BASE_URL=${EDITOR_BASE_URL:-http://nginx/editor}
      - ROSETTA_DOMAIN=${ROSETTA_DOMAIN}
      - ROSETTA_FE=${ROSETTA_FE}
      - COMMUNITY_GROUP_MAPPINGS=${COMMUNITY_GROUP_MAPPINGS:-}
      - GRAPH_SYNC_INTERVAL_HOURS=${GRAPH_SYNC_INTERVAL_HOURS}
      - ADMIN_EMAILS=${ADMIN_EMAILS:-}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - rosetta

  # Backend-Editor Load Balancing - 3 replicas (sharding pattern)
  # Each diagram consistently routes to same instance via nginx URI hashing
  be-editor-1:
    build: ../services/backend-editor
    container_name: be-editor-1
    expose:
      - "3001"
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/rosetta-editor
      - TENANT_ID=${TENANT_ID}
      - CLIENT_ID=${CLIENT_ID}
      - COMMUNITY_GROUP_MAPPINGS=${COMMUNITY_GROUP_MAPPINGS:-}
      - ADMIN_EMAILS=${ADMIN_EMAILS:-}
      - INSTANCE_ID=be-editor-1
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - rosetta

  be-editor-2:
    build: ../services/backend-editor
    container_name: be-editor-2
    expose:
      - "3001"
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/rosetta-editor
      - TENANT_ID=${TENANT_ID}
      - CLIENT_ID=${CLIENT_ID}
      - COMMUNITY_GROUP_MAPPINGS=${COMMUNITY_GROUP_MAPPINGS:-}
      - ADMIN_EMAILS=${ADMIN_EMAILS:-}
      - INSTANCE_ID=be-editor-2
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - rosetta

  be-editor-3:
    build: ../services/backend-editor
    container_name: be-editor-3
    expose:
      - "3001"
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/rosetta-editor
      - TENANT_ID=${TENANT_ID}
      - CLIENT_ID=${CLIENT_ID}
      - COMMUNITY_GROUP_MAPPINGS=${COMMUNITY_GROUP_MAPPINGS:-}
      - ADMIN_EMAILS=${ADMIN_EMAILS:-}
      - INSTANCE_ID=be-editor-3
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - rosetta

  # auth-service handles OAuth flow only (login, callback, logout, refresh)
  # Token validation is done locally by backends
  auth-service:
    build: ../services/auth-service
    expose:
      - "3002"
    environment:
      - OIDC_ISSUER=${TENANT_ID}
      - OIDC_CLIENT_ID=${CLIENT_ID}
      - OIDC_CLIENT_SECRET=${CLIENT_SECRET}
      - OIDC_REDIRECT_URI=http://localhost/auth/callback
      - ROSETTA_DOMAIN=localhost
    networks:
      - rosetta

  mongodb:
    image: mongo:7.0
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - rosetta


  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=rosetta
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d rosetta"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - rosetta

networks:
  rosetta:
    driver: bridge

volumes:
  mongodb_data:
  postgres_data:

# Rosetta Platform - Main Docker Compose
# All services with nginx reverse proxy
#
# ROUTES (via nginx on port 80):
#   /api/*     → backend
#   /editor/*  → be-editor
#   /studio/*  → fe-editor
#   /*         → frontend

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.docker.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - be-editor
      - backend-1
      - backend-2
      - backend-3
      - auth-service
    networks:
      - rosetta
    restart: unless-stopped

  frontend:
    build: ../apps/frontend
    expose:
      - "3000"
    networks:
      - rosetta

  fe-editor:
    build: ../apps/frontend-editor
    expose:
      - "80"
    networks:
      - rosetta

  # Backend Load Balancing - 3 replicas
  backend-1:
    build: ../services/backend
    container_name: backend-1
    expose:
      - "8080"
    environment:
      - RUNNING_IN_DOCKER=true
      - DB_HOST=postgres
      - CLIENT_ID=${CLIENT_ID}
      - TENANT_ID=${TENANT_ID}
      - PG_DB_URL=${PG_DB_URL}
      - EDITOR_BASE_URL=${EDITOR_BASE_URL}
      - ROSETTA_DOMAIN=${ROSETTA_DOMAIN}
      - ROSETTA_FE=${ROSETTA_FE}
      - COMMUNITY_GROUP_MAPPINGS=${COMMUNITY_GROUP_MAPPINGS:-}
      - GRAPH_SYNC_INTERVAL_HOURS=${GRAPH_SYNC_INTERVAL_HOURS}
      - ADMIN_EMAILS=${ADMIN_EMAILS:-}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - rosetta

  backend-2:
    build: ../services/backend
    container_name: backend-2
    expose:
      - "8080"
    environment:
      - RUNNING_IN_DOCKER=true
      - DB_HOST=postgres
      - CLIENT_ID=${CLIENT_ID}
      - TENANT_ID=${TENANT_ID}
      - PG_DB_URL=${PG_DB_URL}
      - EDITOR_BASE_URL=${EDITOR_BASE_URL}
      - ROSETTA_DOMAIN=${ROSETTA_DOMAIN}
      - ROSETTA_FE=${ROSETTA_FE}
      - COMMUNITY_GROUP_MAPPINGS=${COMMUNITY_GROUP_MAPPINGS:-}
      - GRAPH_SYNC_INTERVAL_HOURS=${GRAPH_SYNC_INTERVAL_HOURS}
      - ADMIN_EMAILS=${ADMIN_EMAILS:-}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - rosetta

  backend-3:
    build: ../services/backend
    container_name: backend-3
    expose:
      - "8080"
    environment:
      - RUNNING_IN_DOCKER=true
      - DB_HOST=postgres
      - CLIENT_ID=${CLIENT_ID}
      - TENANT_ID=${TENANT_ID}
      - PG_DB_URL=${PG_DB_URL}
      - EDITOR_BASE_URL=${EDITOR_BASE_URL}
      - ROSETTA_DOMAIN=${ROSETTA_DOMAIN}
      - ROSETTA_FE=${ROSETTA_FE}
      - COMMUNITY_GROUP_MAPPINGS=${COMMUNITY_GROUP_MAPPINGS:-}
      - GRAPH_SYNC_INTERVAL_HOURS=${GRAPH_SYNC_INTERVAL_HOURS}
      - ADMIN_EMAILS=${ADMIN_EMAILS:-}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - rosetta

  be-editor:
    build: ../services/backend-editor
    expose:
      - "3001"
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/rosetta-editor
      - TENANT_ID=${TENANT_ID}
      - CLIENT_ID=${CLIENT_ID}
      - COMMUNITY_GROUP_MAPPINGS=${COMMUNITY_GROUP_MAPPINGS:-}
      - ADMIN_EMAILS=${ADMIN_EMAILS:-}
    depends_on:
      - mongodb
    networks:
      - rosetta

  # auth-service handles OAuth flow only (login, callback, logout, refresh)
  # Token validation is done locally by backends
  auth-service:
    build: ../services/auth-service
    expose:
      - "3002"
    environment:
      - OIDC_ISSUER=${TENANT_ID}
      - OIDC_CLIENT_ID=${CLIENT_ID}
      - OIDC_CLIENT_SECRET=${CLIENT_SECRET}
      - OIDC_REDIRECT_URI=http://localhost/auth/callback
      - ROSETTA_DOMAIN=localhost
    networks:
      - rosetta

  mongodb:
    image: mongo:7.0
    volumes:
      - mongodb_data:/data/db
    networks:
      - rosetta

  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=rosetta
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d rosetta"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - rosetta

networks:
  rosetta:
    driver: bridge

volumes:
  mongodb_data:
  postgres_data:
